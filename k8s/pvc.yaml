---
# k3s 自带 local-path provisioner，单节点场景下 RWO 就够用
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radar-data
  namespace: radar
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
